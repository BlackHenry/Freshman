#include <iostream>
#include <vector>
#include <unordered_map>

using namespace std;

int n;
vector<int> a;
unordered_map<int, bool> mp;

int main()
{
    int i = 1;
    mp.insert(make_pair(1,1));
    while(a.size() < 100){

    }
    return 0;
}
